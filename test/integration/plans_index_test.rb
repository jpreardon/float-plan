require 'test_helper'

class PlansIndexTest < ActionDispatch::IntegrationTest

  test 'admin should see everybodys plans' do
    skip 'Fill this in once the index page is closer being finished'
  end
  
  test 'only active plans should be shown' do
    # Not really sure if we want to do this or not 2019-05-28 JPR
    skip 'Fill this in once the index page is closer being finished'
  end
end
