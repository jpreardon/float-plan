<nav class="navbar navbar-dark bg-dark navbar-expand-sm">
  <div class="container">
    <%= link_to app_name, root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <% if logged_in? %>
          <li class="nav-item">
            <%= link_to 'Float Plans', plans_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Fleet', boats_path, class: 'nav-link' %>
          </li>
          <% if current_user.admin? %>
            <li class="nav-item">
              <%= link_to 'Crew', users_path, class: 'nav-link' %>
            </li>
          <% end %>
          <li class="nav-item">
            <%= link_to 'Sign Out', logout_path, method: 'delete', class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>