<tr>
  <td><%= link_to plan.date.nil? ? '--' : format_date_human(plan.date), plan_path(plan) %></td>
  <td><%= plan.time_out.nil? ? '--' : format_time_human(plan.time_out) %></td>
  <td><%= plan.time_in.nil? ? '--' : format_time_human(plan.time_in) %></td>
  <td><%= plan.direction %></td>
  <td><%= plan.notes_out %></td>
</tr>