<tr>
  <td><%= link_to plan.time_out.nil? ? '--' : plan.time_out.strftime('%d-%b-%Y'), plan_path(plan) %></td>
  <td><%= plan.time_out.nil? ? '--' : plan.time_out.strftime('%l:%M %p') %></td>
  <td><%= plan.time_in.nil? ? '--' : plan.time_out.strftime('%l:%M %p') %></td>
  <td><%= plan.direction %></td>
  <td><%= plan.notes_out %></td>
</tr>